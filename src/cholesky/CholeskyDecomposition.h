#pragma once
#include <cmath>
#include <vector>
using namespace std;

// Should take a matrix as input and decompose it to its lower triangle matrix
vector<vector<double>> CholeskyDecomposition(vector<vector<double>> matrix);
